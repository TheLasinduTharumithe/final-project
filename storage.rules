rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /ads/{restaurantId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null &&
        request.auth.uid == restaurantId &&
        request.resource.contentType.matches('image/.*');
    }
  }
}
